import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
    yandexMapsContainer: {
        color: "white",
        width: "100%",
        ymaps: {
            width: "100%",
            height: "100%"
        }
    },
}))

export default useStyles; 